import java.util.Arrays;

public class ArrayFillMethod {
    public static void main(String[] args) {
        int[] numbers = new int[10]; // Creates an array of size 5

        Arrays.fill(numbers, 40); // Fills all elements with 7

        System.out.println(Arrays.toString(numbers)); // Prints the array
    }
}
